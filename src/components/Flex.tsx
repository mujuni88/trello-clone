/* tslint:disable:no-any*/

import styled from 'styled-components'
import { space, width, fontSize, color, flex, flexDirection, flexWrap, justifyContent, alignItems } from 'styled-system'

export const Flex: any = styled.div`
  ${flex}
  ${flexDirection}
  ${flexWrap}
  ${justifyContent}
  ${alignItems}
  ${space}
  ${width}
  ${fontSize}
  ${color}
`
